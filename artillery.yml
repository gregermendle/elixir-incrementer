config:
  target: "http://localhost:3333"
  phases:
    - duration: 10
      arrivalRate: 40
scenarios:
  - flow:
      - post:
          url: "/increment"
          formData:
            key: "test"
            value: "10"
